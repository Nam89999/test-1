<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.inflearn.mapper.BoardMapper">

	<!-- 게시판 리스트 불러오기 -->
	<!-- as를 이렇게 사용하는게 맞나? -->
	<select id="readboardList" resultType="boardVO">
		select board_id AS
		boardnum,writer,title,contents,count,regdate
		from tb_board2
	</select>

	<!-- 게시판 올리기 (insert) -->
	<insert id="insertBoard" parameterType="boardVO">
		INSERT INTO
		tb_board2(title, contents, count, writer)
		VALUES(#{title},#{contents},#{count},#{writer})
	</insert>

	<!-- 게시물 상세보기 (read) -->
	<select id="readBoardContents" parameterType="Integer"
		resultType="boardVO">
		SELECT board_id AS boardnum,writer,title,contents,count
		FROM tb_board2
		WHERE board_id = #{boardnum}
	</select>


	<!-- 게시물 수정하기 (update) -->
	<update id="updatBoard" parameterType="boardVO">
		update tb_board2 set title=#{title}, contents=#{contents}
		where board_id = #{boardnum}
	</update>
</mapper>